# Fase 3 — GitOps. Il tag pinnato qui vince sul `:latest` placeholder di deployment.yaml: è questo
# file, in Git, a dire quale build gira sul cluster. Bump manuale (= la promozione resta una scelta
# umana). Vedi ui/kustomization.yaml per il ragionamento completo.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: procionemgr-ingestion

resources:
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/markcapitanelli/procionemgr-ingestion
    # [B2, 2026-07-28] Lo stato di sync distingue "aggiornata" da "ferma": prima deduceva la salute
    # dal numero di righe processate, e su una serie morta scriveva "OK: 1 candele" per sempre.
    # [Fase 4, 2026-08-10] Prima promozione a build locale (vedi ui/kustomization.yaml).
    # Precedente (build CI): 562b359db3733aec8f222d76fad59c18ff7aefdd.
    # [2026-08-13] Promozione: budget di ciclo nel MarketDataSyncWorker — una chiamata appesa
    # costa al massimo un ciclo invece di parcheggiare il worker (30 minuti muto, 89 serie oltre
    # tolleranza). Vedi docs/REPORT-INGESTION-PROBE-2026-08-13.md.
    # Precedente: local-cde4dda1.
    # [2026-08-15] Promozione OBBLIGATORIA insieme al deployment.yaml (la liveness passa a
    # /health/live, che esiste solo da questa immagine): il worker non muore più su un timeout di
    # rete letto come shutdown, il ciclo abbandonato non affama le serie successive, e il pod si fa
    # riavviare da solo se il loop si parcheggia. Vedi docs/REPORT-SYNC-WORKER-STALL-2026-08-15.md.
    # Precedente: local-0e97f5ea.
    newTag: local-43340a6c
