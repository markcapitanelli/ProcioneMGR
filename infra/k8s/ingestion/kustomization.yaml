# Fase 3 — GitOps. Il tag pinnato qui vince sul `:latest` placeholder di deployment.yaml: è questo
# file, in Git, a dire quale build gira sul cluster. Bump manuale (= la promozione resta una scelta
# umana). Vedi ui/kustomization.yaml per il ragionamento completo.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: procionemgr-ingestion

resources:
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/markcapitanelli/procionemgr-ingestion
    # [B2, 2026-07-28] Lo stato di sync distingue "aggiornata" da "ferma": prima deduceva la salute
    # dal numero di righe processate, e su una serie morta scriveva "OK: 1 candele" per sempre.
    # [Fase 4, 2026-08-10] Prima promozione a build locale (vedi ui/kustomization.yaml).
    # Precedente (build CI): 562b359db3733aec8f222d76fad59c18ff7aefdd.
    newTag: local-cde4dda1
